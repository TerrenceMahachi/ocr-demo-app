# dependencies
node_modules
npm-debug.log
yarn-error.log
.pnpm-debug.log

# build
.next
dist
out

# envs and secrets
.env
.env.*
apps/api/.env
apps/web/.env.local

# DB + uploads (keep in Docker volumes, not repo)
apps/api/data/*.db
apps/web/public/uploads/*

# system
.DS_Store
*.log
